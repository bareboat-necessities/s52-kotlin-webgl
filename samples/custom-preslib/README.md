# Custom Presentation Library example

Use explicit runtime wiring when your application generates or loads its own `PresLibPack`.

The sample still uses the synthetic pack so it can be copied without external assets. Replace `PresLibPack.phase2Synthetic()` with your generated pack.

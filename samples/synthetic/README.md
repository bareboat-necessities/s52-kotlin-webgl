# Synthetic samples

This directory is reserved for small synthetic feature fixtures used by the demo and golden tests.

Phase 0 keeps fixtures in Kotlin code so the project has no parser dependency.
